# image

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build image` to build the library.

## Running unit tests

Run `nx test image` to execute the unit tests via [Jest](https://jestjs.io).
